server:
  port: 8082
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/
spring:
  application:
    name: travel-web
  thymeleaf:
    encoding: UTF-8
    servlet:
      content-type: text/html
redis:
  host: localhost
  port: 6379
  jedis:
  pool:
    max-idle: 300
    max-active: 600
    max-wait: 300

